{
  "$schema": "https://railway.app/railway-schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan storage:link && php artisan migrate --force && php -d upload_max_filesize=20M -d post_max_size=25M -S 0.0.0.0:$PORT -t public/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}